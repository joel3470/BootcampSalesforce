# Directorio para almacenar imágenes descargadas

Este directorio contiene las imágenes de perfil descargadas desde la API de RandomUser.me.

Las imágenes se nombran con el formato: `user_{id}_{timestamp}.jpg`
